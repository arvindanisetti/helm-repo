apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jairam.fullname" . }}-test-connection"
  labels:
    {{- include "jairam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jairam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
